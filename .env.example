# Copy to .env and fill in. .env is gitignored.

# Required for final grounded generation (and the default judge model).
OPENAI_API_KEY=sk-your-key-here

# Optional: local judge via Ollama (Phase 3+). Leave commented unless using Ollama.
# OLLAMA_BASE_URL=http://localhost:11434
