"""Production RAG — Hybrid Search + Semantic Cache + Auto-Eval.

Modular monolith. Module boundaries are intentionally clean so each concern can
later be lifted into its own service (see Phase 8 of the master plan).
"""

__version__ = "0.1.0"
